{
    "name": "@pizza/frontend",
    "version": "0.0.1",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "start": "parcel serve",
        "build": "parcel build --no-autoinstall --dist-dir ../../dist/ --no-cache --no-scope-hoist --public-url $APP_PUBLIC_URL"
    },
    "targets": {
        "default": {
            "source": "./src/index.html"
        },
        "main": false
    }
}
