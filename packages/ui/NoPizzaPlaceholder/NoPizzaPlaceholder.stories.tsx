import { createStory } from "@pizza/utils/createStory";
import { NoPizzaPlaceholder } from "./NoPizzaPlaceholder";

const { Template, meta } = createStory(NoPizzaPlaceholder, {
    title: "components/No Pizza Placeholder",
});

export const NoPizzaPlaceholder_ = Template.bind({});

export default meta;
