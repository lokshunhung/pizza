import { createStory } from "@pizza/utils/createStory";
import { OrderSuccess } from "./OrderSuccess";

const { Template, meta } = createStory(OrderSuccess, {
    title: "components/Order Success",
});

export const OrderSuccess_ = Template.bind({});

export default meta;
