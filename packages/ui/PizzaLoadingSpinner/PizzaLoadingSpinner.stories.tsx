import { createStory } from "@pizza/utils/createStory";
import { PizzaLoadingSpinner } from "./PizzaLoadingSpinner";

const { Template, meta } = createStory(PizzaLoadingSpinner, {
    title: "components/Pizza Loading Spinner",
});

export const PizzaLoadingSpinner_ = Template.bind({});

export default meta;
